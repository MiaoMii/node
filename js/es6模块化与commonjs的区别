#### commonjs模块输出是一个值得拷贝，ES6模块输出是值得引用

commonjs： 一旦输出一个值，模块内部得变化就影响不到这个值

ES6：遇到模块加载命令import，就会生成一个值引用。js脚本执行时，再根据这个只读引用时，再到被加载的模块里面去取值。<font color=red>ES6模块时动态引用</font>。

#### commonjs模块时运行时加载，ES6模块是编译时输出接口

+ 运行时加载： commonjs模块就是对象；即在输入时是在加载整个模块，生成一个对象，然后再从这个对象上面读取方法
+ 编译时加载：ES6模块不是对象，而是通过export命令显示指定输出的代码，import时采用静态命令形式。即在import时可以指定加载某个输出值，而不是加载整个模块



commonjs加载的是一个对象，只会在脚本运行结束时生成。ES6再代码静态解析阶段就会生成